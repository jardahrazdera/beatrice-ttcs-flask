# Environment Configuration Example
# Copy this file to .env and adjust values for your environment

# Flask Configuration
FLASK_DEBUG=false
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Evok API Configuration
EVOK_HOST=127.0.0.1
EVOK_PORT=8080

# Development Mode
# Set to 'true' to use mock Evok client for testing without hardware
USE_MOCK_EVOK=false

# Super Admin Password
# Required for sensitive operations (manual override, database deletion)
# IMPORTANT: Change this in production!
# Option 1: Provide plain text password (will be hashed automatically)
SUPER_ADMIN_PASSWORD=superadmin123

# Option 2: Provide pre-hashed password (SHA-256) for added security
# Generate hash: python3 -c "import hashlib; print(hashlib.sha256('your-password'.encode()).hexdigest())"
# SUPER_ADMIN_PASSWORD_HASH=your-sha256-hash-here

# Production Settings
# Uncomment and configure for production deployment
# EVOK_HOST=192.168.1.100
# SECRET_KEY=your-secure-random-key-here
# USE_MOCK_EVOK=false
# SUPER_ADMIN_PASSWORD=your-secure-super-admin-password
